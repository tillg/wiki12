{
  "name": "@wiki12/cli",
  "version": "0.1.0",
  "description": "wiki12 CLI — content CRUD, model/form management, and migrations over the A12 Data Service + model-lifecycle service.",
  "type": "module",
  "license": "EUPL-1.2",
  "bin": { "wiki12": "src/index.ts" },
  "scripts": {
    "start": "node --experimental-strip-types src/index.ts",
    "test": "node --test --experimental-strip-types test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5.6"
  }
}
